# 缓存
## 缓存header
### 强缓存
- Expires：缓存过期时间，绝对时间，已被 Cache-Control 替代。
- Cache-Control：缓存指令，常用指令有 max-age（缓存时间）、no-cache（需要验证缓存）、no-store（不缓存）等。
### 协商缓存
- Last-Modified：资源最后修改时间，与 If-Modified-Since 配合使用。
- ETag：资源唯一标识符，与 If-None-Match 配合使用。